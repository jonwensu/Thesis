{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ImageAnnouncement list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>Dateposted</th>
                <th>Prioritylvl</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('announcement_image_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.content }}</td>
                <td>{% if entity.datePosted %}{{ entity.datePosted|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.priorityLvl }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('announcement_image_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('announcement_image_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('announcement_image_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
